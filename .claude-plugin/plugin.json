{
  "name": "readless",
  "version": "0.4.0",
  "description": "Speak Claude Code activity out loud while you're AFK — agent-callable speak_summary / speak_status / speak_blocker tools. Default backend is edge-tts (free, online, bilingual, no API key).",
  "mcpServers": {
    "readless": {
      "command": "python3",
      "args": ["-m", "readless.server"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src"
      }
    }
  }
}
