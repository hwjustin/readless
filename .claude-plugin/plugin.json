{
  "name": "readless",
  "version": "0.3.0",
  "description": "Speak Claude Code activity out loud while you're AFK — agent-callable speak_summary / speak_status / speak_blocker tools via OS-native or API TTS.",
  "mcpServers": {
    "readless": {
      "command": "python3",
      "args": ["-m", "readless.server"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src"
      }
    }
  }
}
